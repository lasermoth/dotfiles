# Secrets

Do not commit private keys, tokens, passwords, or machine-specific secrets to this repository.

Future options for managed secrets:

- `sops-nix`
- `agenix`
- 1Password CLI-backed setup

For now, secrets should be restored manually or managed outside this repo.
